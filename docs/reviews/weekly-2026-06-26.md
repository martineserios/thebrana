## Weekly Review — 2026-06-26

**Period:** 2026-06-19 → 2026-06-26

---

### Health

| Metric | Value | Δ vs Jun-19 | Status |
|--------|-------|-------------|--------|
| Commits this week | 120 | +57 | green |
| Tasks completed this week | 2 | -17 | yellow |
| Tasks in progress | 7 | +7 | yellow |
| Unblocked P0/P1 pending | 24 | — | yellow |
| Blocked tasks | 425 | +321 | red |
| Pending backlog | 526 | +37 | yellow |
| Zombie candidates (never started, >30d) | 337 | +36 | red |

**High commit, low explicit task closure.** 120 commits in two days (Jun 20–21) represent a dense multi-feature sprint, but only 2 tasks formally closed. The commit-to-close gap suggests either tasks were completed without marking, or most work was infrastructure that doesn't map to the existing schema. Blocked task count jumped significantly — likely a measurement artifact from improved blocked_by population rather than new blockers.

---

### Shipped

**ADR-060 / Dev-First Integration Model** (t-2188) — the week's centrepiece. Reclassified `dev` as the integration branch and `main` as release-only. Wired through `brana close`, `brana deploy`, `bootstrap.sh`, CLAUDE.md, and hooks reference. Field note added: Step 11b must not reap worktrees sitting at `main` HEAD (active lanes hazard).

**Multi-Agent Substrate + Autonomous Runner** (ADR-059 cluster) — full runner stack:
- `t-2146` — worktree isolation + per-project `RUNNER_BASE_BRANCH`
- `t-2144` — `--run-batch` concurrency lock via flock
- `t-2150` — safety-invariant test suite
- `t-2149` — graceful StructuredOutput degradation
- `t-2143` — substrate design completion: learned eligibility, consensus, capstone
- `t-2167` — first real-token calibration + when-to-sweep heuristic

**Backlog concurrency hardening** (t-2166) — sidecar lock (`.json.lock`) serialises tasks.json writes across concurrent sessions. Removes race condition.

**Per-repo backlog scoping** (t-2155 / t-2157–t-2160 cluster):
- `tasks-config.json` scoped per git root — stops `active_epic` bleeding across repos
- Cross-project task creation via `--project` / `project` field
- Gitignored per-repo config + user guide

**Infrastructure fixes:**
- `t-2177` — `context-budget.sh` single-sourced; `validate.sh` + pre-commit delegate to it
- `t-2138` — secret-scan pre-commit + PreToolUse gate
- `t-2148` — stale-branch reaper (merged-only, never touches unmerged)
- `t-2154` — session handoff: unit-key routing + close-from-main guard
- `t-2156` — RTK compression registered as PreToolUse hook
- `t-2171` — Go language evaluation research spike completed

---

### In Progress (watch list)

| ID | Epic | Subject | Age |
|----|------|---------|-----|
| t-1766 | ruflo-integration | Analyze ruflo orchestration surface | 27 days |
| t-1103 | — | CC Alignment Program | never started |
| t-1463 | — | Unified Memory Write Gateway | never started |
| in-001 | backlog-ui | Backlog UI | open-ended |
| in-002 | cc-alignment | CC Alignment | open-ended |
| in-003 | memory-arch | Memory Architecture | open-ended |
| in-004 | rust-cli | Rust CLI (brana-core + brana-cli + brana-mcp) | open-ended |

`t-1766` is 27 days old — needs a closure commit or scope reduction. The four `in-*` tasks appear to be epic-level trackers; they don't have start dates and likely won't close until sub-tasks do.

---

### Blockers / P0 Priorities

**3 P0 tasks, all unblocked:**

| ID | Epic | Subject |
|----|------|-------|
| t-1999 | reminders | Reminders delivery (ADR) |
| t-2028 | knowledge-pipeline | ADR-042 amendment — frontmatter `type:` on ingest output |
| t-2029 | knowledge-pipeline | Absorb/supersede 6 overlapping idea docs |
| t-2074 | reminders | ADR: integral temporal model |

**Top unblocked P1 clusters:**

- **backlog-git-alignment**: t-2164 (per-repo focus scope), t-2165 (epic field at close), t-2168 (flock + git merge driver) — natural follow-on from t-2166 lock work
- **knowledge-pipeline**: t-2021 (Knowledge Base Redesign), t-1583 (agy_delegate MCP tool), t-1781 (process-url CLI)
- **ruflo-integration**: t-1587 (Phase 1 wiring layer)

---

### Zombies

337 tasks pending, never started, older than 30 days (up from 301 on Jun-19, growing ~35/week).

Growing faster than triage. Key structural blocker: `ms-010` / `ms-011` chain blocks 8+ research-pipeline tasks that have been queued since early spring. These should either be cleared (ms-010 completed?) or cancelled if the NotebookLM dependency is stale.

---

### Next Week

**Highest leverage:**
1. **t-2028 + t-2029** (knowledge-pipeline P0) — both unblocked, S-effort fixes. Close them.
2. **t-2074** (reminders P0 — temporal model ADR) — unblocked, needed to unblock t-2075 + t-1999.
3. **t-2168** (backlog-git-alignment P1) — flock + git merge driver; completes the concurrency hardening trilogy (t-2166 → t-2168).
4. **t-1766 closure** — either ship the ruflo analysis doc or cancel; 27 days is too long for a research spike.
5. **ms-010 triage** — determine if NotebookLM MCP is available; if yes, unblock 8+ tasks. If no, cancel the chain.

**Zombie triage pass** is overdue — schedule a focused `/brana:backlog triage-stale` session before the backlog hits 600 pending.

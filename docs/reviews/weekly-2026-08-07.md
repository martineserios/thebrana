## Weekly Review — 2026-08-07

**Period:** 2026-07-31 → 2026-08-07

---

### Health

| Metric | Value | Δ vs Jul-24 | Status |
|--------|-------|-------------|--------|
| Commits this week | 80 | -21 | yellow |
| Tasks in progress | 8 | -2 | green |
| Unblocked P0 pending | 1 | -2 | yellow |
| Total pending backlog | 707 | +127 | red |
| Blocked pending | 0 | -112 | green |
| Stale in-progress (>30d no output) | 2 | 0 | red |

**Stabilization week.** 80 commits (down from 101), heavily weighted to fix + chore (52 of 80). Infrastructure reliability was the theme: ruflo upgrade, MCP hang fixes, oracle-hub currency, session close bugs. Two P0/P1 must-do items from last review were closed (t-2028, t-2029); t-2214 cancelled. The P0 temporal model ADR (t-2074) remains pending for the fourth consecutive week.

Backlog grew +127 in one week (580 → 707) — fastest single-week growth observed. This is likely task filing from the burst sprint, but it signals the accumulation rate has accelerated.

---

### Shipped

**ruflo v3.34.0 upgrade (t-2627, t-2632–t-2636)**
Full upgrade from v3.10.39 across global install and both wrappers. Memory reliability re-verified: round-trip test, corruption-check logic re-evaluated against new binary, ADR-377 retrieval guard env var recorded. Regression test suite added (t-2634).

**brana MCP server reliability (t-2631)**
`spawn_blocking` sweep extended to all remaining tool handlers — `backlog_search`, `session_history`, `backlog_add` were hanging then failing. Matching handler tests added.

**Build receipts (t-2593, t-2592, ADR-076)**
`brana receipt mint|validate` shipped as executed evidence. Receipts are cryptographically tied to a specific tool execution — the "challenger gate false-positive" pattern that surfaced in t-2627 (noted in the commit) is the motivating case.

**Session close reliability (t-2603, t-2618, t-2617)**
Three P1 close/hook bugs resolved in one session: (1) LAST_CLOSE anchor now scoped to own session's first commit; (2) Tier 0 epic focus globality fix — concurrent sessions no longer misroute into each other's epics; (3) GIT_DIR leak sanitized from all git subprocesses in hooks.

**Knowledge reindex P0 fix (t-2613)**
Orphan cleanup was hard-deleting all non-doc entries (url, feed, research). Fixed — `index_memory` now drops only files with no `{type}_` filename prefix.

**Scheduler fix (t-2611)**
`TimeoutStartSec` bumped to cover the full flock wait — was killing lock-waiting jobs before the 600s grace period expired. Derivation documented.

**Oracle-hub currency (t-2501)**
`oracle-hub brana ship` + drift check scripts wired. Scheduler job registered. Previously oracle-hub was silently running 2-day-stale code.

**LinkedIn pipeline (t-2568, t-2589, t-2560)**
Tier-2 fetch timeout fixed; tiers inverted (public JSON-LD/og as primary, authenticated scrape as fallback); `research-extraction.sh` retired and scheduler re-enabled. Pipeline is now operational for the link drain queue.

**Ship deferral hypothesis falsified (t-2547)**
Investigation confirmed no deferral loop — shipping ran ~twice/day through late July. Filed and closed in the same session.

**Context instrumentation (t-2591)**
Churn-vs-understanding token split added to build context. Quota burn tracking in place.

---

### In Progress (watch list)

| ID | Subject | Age | Note |
|----|---------|-----|------|
| t-2619 | ruflo-mcp.sh rotation discards healthy DBs | ~1 week | P1, S-effort — close this week |
| t-2506 | session write same-day merge silently discards text | ~1 week | P1, S-effort — close this week |
| t-2249 | thebrana-web: commercial landing page rework | 4+ weeks | P1 commercial work — zero visible progress in 4 weeks |
| t-2173 | Sandbox autonomous runner executor | 4+ weeks | P2, large — may be blocked post-v3 migration |
| t-1766 | Analyze ruflo orchestration surface | 45+ days | **Fifth review. Cancel or extract notes now.** |
| t-1103 | CC Alignment Program | long-running | P1 program tracker — expected |
| t-1463 | Unified Memory Write Gateway | long-running | P1 — no recent commits |
| t-2622 | Wire hook/oracle tests into validate.sh | new | P3, M-effort |

`t-1766` has appeared in every review since at least June. If there's no formalized output (doc, ADR, pattern), the slot should be freed. Cancel and write one markdown note to memory if there are durable findings.

`t-2249` (commercial landing page) is the only customer-facing P1 with no progress for 4 weeks. The ruflo/MCP sprint is done — this is the natural next slot to fill.

---

### Blockers / P0 Priorities

**Single P0 remaining:**

| ID | Subject | Effort | Week # |
|----|---------|--------|--------|
| t-2074 | ADR: integral temporal model | S | **Week 4** |

t-2074 is unblocked, S-effort, and blocks t-2075 (Stage 0 soak gate) and t-1999. Four consecutive reviews. This is a decision, not a task: either schedule two focused hours or explicitly deprioritize (cancel / defer to a milestone). Carrying it as "next week" indefinitely has a cost — t-2075 can't start.

**Two P1 quick fixes in-flight (should not roll to next week):**
- t-2619: ruflo-mcp.sh rotation fix — already in-progress, S-effort
- t-2506: session write merge bug — already in-progress, S-effort

---

### Backlog Health

Total pending: **707** (up +127 from 580). All pending tasks currently show no epic tag — the epic taxonomy from backlog-v3 migration may not have fully propagated to the pending queue. Zombie triage is overdue.

**Actions needed:**
- Run `/brana:backlog triage-stale` — estimate 150–200 cancellable tasks in the zombie pool
- Audit why pending grew +127 in one week; distinguish new filings vs migrated tasks vs false-positive counts
- Verify epic field propagation on pending tasks (all showing `(none)`)

---

### Knowledge Health

The 2026-08-01 knowledge review (separate doc) surfaced:
- 692 broken internal links (328 are cross-repo — structural, won't resolve in this clone)
- 13 unfilled template placeholders in live docs
- 139 true orphan nodes (no spec-graph edges)
- Spec graph grew to 536 nodes / 2,000 edges (healthy growth)

**Actionable:** The 13 unfilled placeholders are a quick pass. Cross-repo links (328) need a documentation convention decision — either use relative paths with a note, or convert to explicit external references.

---

### Last Review Action Tracking

| Item | Status |
|------|--------|
| t-2028 ADR-042 amendment | ✓ Completed |
| t-2029 Absorb 6 idea docs | ✓ Completed |
| t-2074 Temporal model ADR | ✗ Still pending (week 4) |
| t-2214 Close as done | ✓ Cancelled |
| t-1766 Resolve or cancel | ✗ Still in-progress (week 5) |
| t-2249 Restart commercial work | ✗ No progress |
| cli/kb-redesign zombie triage | ✗ Not done |

4 of 7 items actioned — better than last week. Two carry-forwards (t-2074, t-1766) are now in their 4th and 5th cycles respectively.

---

### Next Week

**Must-do (overdue):**

1. **Close t-2074** (temporal model ADR, S-effort, P0) — week 4. Block 2 hours, write the ADR, done. Unblocks t-2075 and t-1999.
2. **Close t-2619 + t-2506** (S-effort P1 fixes, both in-progress) — should already be done; don't let these carry.
3. **Resolve t-1766** — cancel with a one-paragraph memory note, or produce the output. Week 5.

**High-leverage next actions:**

4. **t-2249 commercial landing page** — the infrastructure sprint is over. This is the P1 business task with the longest stall. Allocate a focused session.
5. **Backlog zombie triage** — 707 pending tasks, growing. One `/brana:backlog triage-stale` session could cancel 150+ items and reduce noise. The +127 growth in one week needs investigation.
6. **Knowledge link cleanup** — 13 template placeholders are a 30-minute pass. Decide on cross-repo link convention (the 328 structural broken links).
7. **t-2276** (wire doc-graph into `/brana:reconcile --scope propagation`) — unblocked since t-2275 shipped in July; small task, high architectural payoff.

## Weekly Review — 2026-09-11

**Period:** 2026-09-04 → 2026-09-11
**Note:** Last weekly review was 2026-07-24 — 7-week gap. This review anchors to the current week's activity. Tasks.json is mid-migration (ADR-094 moves the ledger out of the working tree); task velocity derived from git commit messages instead.

---

### Health

| Metric | Value | Δ vs 2026-07-24 | Status |
|--------|-------|-----------------|--------|
| Commits this week | 127 | +26 | green |
| Task IDs touched | 30 | — | green |
| Confirmed completions (explicit state commits) | 5 (t-1706, t-3316, t-3317, t-3318, t-3320) | — | yellow |
| P0 trifecta status (t-2028, t-2029, t-2074) | unknown | 7+ weeks stale | red |
| Backlog flow | no data | — | — |
| Zombies | unknown (was 541 at Jul-24) | — | red |

**Concentrated sprint.** 127 commits over 7 days with 72 landing on 2026-09-07 alone. The burst pattern (single monster day, quiet otherwise) continues from July. Core themes: knowledge enrichment pipeline shipped, ADR-094 decided, LEARN worker wave 2 advanced, CI hardened.

---

### Shipped

**Knowledge enrichment chain — link-signal scoring (t-3306–t-3314)**
Full relevance pipeline now operational. ADR-093 amended to clarify that backlog fill is a queue and scoring runs post-sync. Project descriptors and `project_vectors` table added for scoring (t-3307). `score_relevance` function + enrichment columns (t-3308–t-3310). Post-sync relevance pass + `entities`/`action_type` extraction (t-3311–t-3312). `brana knowledge relevant <project|thebrana>` command shipped (t-3313). Enrichment documented (t-3314).

**LEARN worker v3 wave 2 (t-2404, t-2405)**
Core drain loop implemented (t-2405) with two challenger iterations hardening queue error handling. Compute-chain contract and plan-step context forwarding documented (t-2404). `claude_fallback` call-site count corrected; `t-2405` marked shipped.

**ADR-094: tasks.json ledger relocation (t-3322)**
Drafted, challenged, amended (post-merge backstop, Check 74 as lint, anchored ls-files gate), and accepted. Live ledger moves from the git working tree into `$GIT_COMMON_DIR/brana/`. Interim: hourly rotating backup of ledger in place (t-3326) with hardened path-keyed dir and 700/600 perms. Bootstrap production guard now also requires `local main == origin/main`.

**Reconcile PROP-3 — Could Adopt scan (t-1706)**
`/brana:reconcile` now includes a dimension Could Adopt scan in its propagation scope. Field notes documented.

**CI / validate hardening (t-3319, t-3327, t-3329, t-3341)**
Required-tests check (t-3319) promoted; CI now enforces it. Check 74 hardened — substring 'worktree' exemption dropped, list-style instructions caught, REMEDY_REGISTRY entry added (t-3327). Gate 3 now has a non-overridable finding class: a ship that removes a safety mechanism before its replacement lands is blocked regardless of override flags (t-3329, ADR-094 d7). Unsupported `"if"` keys removed from hooks.json (t-3341).

**Worktree-gate and post-tasks-validate fixes (t-3285, t-3286)**
`worktree-gate.sh` carve-out for dirty-check retired. `post-tasks-validate.sh` updated to use lock_tasks/common-root resolution, closing the `--file` bypass and unlocked hook fallback. Stale git-restore undo hints corrected for checks 62/63/64.

**Backlog / skills housekeeping**
MCP `backlog_add` now used instead of shell-interpolated CLI for epic-slug creation in `done-and-add.md` and PROP-3 (t-3320). Session-start.sh: `$BRANA` override tier added to `resolve-brana.sh`; lane-identity user guide updated (t-3299). ADR-091 amended-by ADR-094 pointer added; stale git-tracked claims fixed (t-3288).

---

### In Progress (watch list)

| ID | Subject | Note |
|----|---------|------|
| t-3287 | bootstrap tasks.json restore test | SKIP'd in CI pending this task; unblocks CI cleanup |
| t-3284 | Check-70 RED tests | Tests held off ship; needs resolution |
| t-2521 | Session state task | Ongoing state commits — check if this is a tracker or a ghost |
| t-3299 | Lane-identity user guide | Docs partially landed; check if complete |

---

### Blockers / P0 Priorities

**P0 trifecta — 7+ weeks stale, status unknown:**

| ID | Subject | Last seen | Note |
|----|---------|-----------|------|
| t-2028 | ADR-042 amendment — `type:` frontmatter on ingest output | Jul 24 | Blocks doc-graph structural correctness |
| t-2029 | Absorb/supersede 6 overlapping idea docs | Jul 24 | Blocks knowledge-pipeline coherence |
| t-2074 | ADR: integral temporal model | Jul 24 | Blocks t-2075 (Stage 0 soak gate) and t-1999 |

These were listed as "must-do" in the July 24 review with the note that all three are S-effort and fully unblocked. Seven weeks without progress is now a signal — either deprioritize explicitly or schedule a single focused session to close all three (estimated: 2–3 hours).

**Commercial landing page (t-2249):** Last noted Jul 24, P1 business work with no git evidence of activity this week either.

---

### Backlog Flow

Backlog flow: no data — stale-lifecycle-report.jsonl hasn't been generated. Run `system/scripts/stale-lifecycle.sh` to produce it, or check if the scheduler job is enabled.

---

### Knowledge Pipeline

Knowledge pipeline enrichment is now operational (shipped this week). No drafts check performed — `brana-knowledge/` not accessible in this session.

---

### Next Week

1. **Close the P0 trifecta** (t-2028, t-2029, t-2074) — all S-effort, fully unblocked, 7+ weeks overdue. Block a 2-hour session, close all three.
2. **Resolve t-3287** (bootstrap restore test) to unblock CI cleanup and lift the SKIP.
3. **Triage t-3284** (Check-70 RED tests held off ship) — currently parked; determine if this blocks the next ship or can be deferred with a filed task.
4. **Commercial landing page (t-2249)** — P1 business work with no visible progress in 7+ weeks. If it's been superseded or deprioritized, close it explicitly.
5. **Enable stale-lifecycle-report job** to restore backlog flow visibility — 541 zombies were flagged in July; without the report, drift is invisible.

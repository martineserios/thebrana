## Weekly Review — 2026-07-24

**Period:** 2026-07-17 → 2026-07-24

---

### Health

| Metric | Value | Δ vs Jul-03 | Status |
|--------|-------|-------------|--------|
| Commits this week | 101 | +86 | green |
| Tasks completed this week | 34 | +30 | green |
| Tasks in progress | 10 | +1 | yellow |
| Unblocked P0 pending | 3 | 0 | red |
| Blocked pending | 112 | +1 | yellow |
| Total pending backlog | 580 | +34 | yellow |
| Zombies (pending, never started, >30d) | 541 | — | red |

**Burst week.** 101 commits, 34 completions — the densest sprint in months. Nearly all work was concentrated on 2026-07-22 (66 commits in one day) around the ADR-065 / backlog-v3 migration. The P0 trifecta remains untouched for a third consecutive week.

---

### Shipped

**ADR-065: backlog-v3 schema — epic-as-top entity (t-2283, t-2284, t-2312–t-2317)**
The major structural migration landed. Epics are now first-class task nodes; the flat `.epic` and `.level` fields are retired; `stream` field removed from 72 live tasks; wave process object added; config cleaned up. Migration script (`collapse-level-epic-v3.py`) ran with `--write` on 2026-07-23. Docs, domain glossary, and user guide updated in the same session.

**Active-epic scoping fix (t-2281, t-2295, t-2296, t-2297, t-2298)**
`active_epic` resolution tightened to project-scoped paths only, eliminating cross-project leakage. `cmd_set_active` now hard-stops when no project root is determinable. `sync-state.sh cmd_pull` gains the contamination guard mirroring `cmd_push`. Migration audit script ran and confirmed clean state.

**Schema sealing bundle (t-2308–t-2311)**
Version-gate floor bumped to 3 with forward-only value validation. Tags normalized (non-array values fixed + blocking `validate.sh` check). Write-path sealing on `level`/`epic` across `set_field`, `cmd_add`, `inherit_initiative`, and MCP `backlog_add`. Key:value tag parser with `--tag key:value` query support.

**Oracle-hub pipeline (t-2306, t-2318, t-2321, t-2324, t-2327, t-2328)**
Telegram link-capture pipeline wired and verified end-to-end. Fixed: brana CLI missing on oracle-hub, bot identity mismatch pointing at wrong Telegram bot, `process-link-queue.sh` dedup false-positive that skipped every link, pre-push hook `OWNED_PATHS` missing `deploy/oracle-state/` (was blocking all oracle-hub pushes).

**Bootstrap / hooks deploy (t-2270)**
`make hooks-deploy` added to `bootstrap.sh` and ship procedure. Hooks were previously skipped during deploys — silent gap now closed.

**Doc-graph spike + overlay (t-2274, t-2275)**
Deterministic doc-graph from wikilinks and frontmatter edges built and merged into `graphify`'s `graph.json`. Foundation for `--scope propagation` in `/brana:reconcile`.

**Backlog-v3 post-migration fixes (t-2322, t-2375, t-2377)**
Three targeted fixes for issues surfaced by the v3 migration: `epic`/`initiative` added to `validate_schema()` type whitelist; close skill Tier 2a/2b detection updated to walk parent chain (not retired flat `.epic`); `TaskFilter.epic` in brana-core fixed to resolve via parent chain.

---

### In Progress (watch list)

| ID | Subject | Age | Note |
|----|---------|-----|------|
| t-2249 | thebrana-web: commercial landing page | 3+ weeks | P1 commercial work — no visible progress this week |
| t-2173 | Sandbox autonomous runner executor | 3+ weeks | Was active; check if blocked by v3 migration |
| t-1766 | Analyze ruflo orchestration surface | 40+ days | **Third warning** — close or scope down immediately |
| t-2214 | Ship dev→main (scaffolder ADR) | stale | **The ship happened 2026-07-02 (commit 1631ec3). Mark done.** |
| in-001–004 | Epic trackers (Backlog UI, CC Alignment, Memory Arch, Rust CLI) | open-ended | Healthy trackers |
| t-1103 | CC Alignment Program | never started | Long-running |
| t-1463 | Unified Memory Write Gateway | never started | Long-running |

`t-2214` is a 30-second housekeeping item — this is the fourth review it has appeared in. Close it.

`t-1766` (ruflo orchestration analysis) is now 40+ days in-progress with no output. The corruption work from June generated a separate cluster of fixes. If the analysis was never formalized, cancel and extract any durable notes into memory.

---

### Blockers / P0 Priorities

**P0 trifecta — unblocked, three weeks untouched:**

| ID | Subject | Effort | Note |
|----|---------|--------|------|
| t-2028 | ADR-042 amendment — frontmatter `type:` on ingest output | S | Both doc-graph (t-2275) and schema-sealing work make this more urgent |
| t-2029 | Absorb/supersede 6 overlapping idea docs | S | Direct blocker for knowledge-pipeline coherence |
| t-2074 | ADR: integral temporal model | S | Blocks t-2075 (Stage 0 soak gate) and t-1999 |

Three consecutive weeks of noting these as "next week" items without scheduling is a pattern worth naming. All three are S-effort and unblocked. If they're not being done, either (a) deprioritize them explicitly or (b) block 2 hours and close them in one session.

t-2028 and t-2275 are now coupled: the doc-graph overlay landed, and the ingest output schema is the missing piece to make it structurally correct. Shipping t-2028 unlocks downstream consistency.

---

### What the v3 Migration Unblocked

The backlog-v3 sprint (ADR-065) was the right call. It resolves a category of contamination bugs and sets up the write-path sealing needed before expanding MCP tooling. However, the follow-on fix pattern (t-2375, t-2377, stale MCP server lag) suggests a brief stabilization pass is worthwhile before the next structural change.

**Immediately shippable follow-ons:**
- `t-2276` — wire doc-graph overlay into `/brana:reconcile --scope propagation` (unblocked by t-2275, which just shipped)
- `t-2291` — graduate `brana backlog ac-propose` into `/brana:ac-propose` skill (blocked by t-2278 — check t-2278 status)

---

### Backlog Health

Total pending: **580** (up +34). Zombies: **541** — tasks that are pending, never started, and older than 30 days. This represents 93% of all pending tasks, which indicates the backlog has become a graveyard for unscheduled ideas rather than a working queue.

**Top zombie epics:**

| Epic | Count |
|------|-------|
| cli | 36 |
| ? (untagged) | 29 |
| kb-redesign | 28 |
| scheduler | 26 |
| web-ui | 21 |
| ideation | 20 |
| ruflo | 18 |
| linkedin | 17 |
| memory | 15 |
| claude-code | 14 |

`cli` (36) and `kb-redesign` (28) are notable: `cli` contains Rust CLI tasks that are actively being worked as an initiative (in-004) — many of these may be superseded by or dependencies of the Rust rewrite. `kb-redesign` (28) likely predates the current knowledge architecture direction and may be cancellable in bulk.

---

### Next Week

**Must-do (overdue):**

1. **Close t-2028 + t-2029** (S-effort each, fully unblocked) — fourth week listing these. Schedule them first.
2. **Close t-2074** (temporal model ADR, S-effort) — unblocks t-2075 and t-1999.
3. **Close t-2214** (30-second housekeeping — the ship happened July 2).
4. **Resolve t-1766** — 40+ days. Close with notes or cancel. Don't carry it to a fifth review.

**High-leverage next actions:**

5. **Ship t-2276** (doc-graph → reconcile integration) — natural successor to the t-2275 spike; small task, big payoff for drift detection.
6. **t-2249 (commercial landing page)** — P1 business work with no recent progress. If the v3 sprint was the reason it stalled, now's the window to restart.
7. **Zombie triage pass: cli + kb-redesign** — 64 tasks between them. A 30-minute `/brana:backlog triage-stale` pass could cancel 20–30 superseded items and reduce backlog noise.
